{
    "version": 2,
    "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
    "build": {
      "env": {
        "INSTALL_COMMAND": "npm install"
      }
    },
    "builds": [{ "src": "api/app.js", "use": "@vercel/node" }],
    "outputDirectory": "."
  }
  